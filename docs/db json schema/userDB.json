{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "http://example.com/product.schema.json",
    "title": "user",
    "description": "Info about a single user",
    "type": "object",
    "properties": {
        "_id": {
          "description": "The identifier for a tenant, for internal use within the database. Globally unique within collection",
          "type": "string"
        },
        "id": {
            "description": "The identifier for a tenant. Globally unique within collection",
            "type": "string"
          },
        "name": {"type": "string"},
        "login": {
            "description": "Login credentials. Globally unique within collection",
            "type": "string"
        },
        "salary": {"type": "number"}
    },
    "required": [ "_id", "name", "id", "login", "salary"]
  }